.DS_Store
.build/
.swiftpm/
DerivedData/
xcuserdata/
*.xcuserdatad/
/Scribe.xcodeproj

# Release artifacts (a DMG and a 1.5 GB DerivedData tree — never committed).
build/
dist/

# Credentials — never commit (repo is public).
hf_token.txt
*token*.txt
