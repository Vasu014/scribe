# Task T9 — Release engineering: Sparkle, notarization, docs

Read AGENTS.md, then SPEC.md §6 (Distribution & Updates) + §7 milestones (signed builds
week). Consult `.agents/skills/macos-release/SKILL.md` and
`.agents/skills/macos-auto-update/SKILL.md` and `.agents/skills/macos-build/SKILL.md`
BEFORE writing scripts — follow their patterns where they apply.

EFFICIENCY: `make build` once at end; short report ≤ 20 lines.

## Deliverables

1. **Sparkle 2 (SPM)**: add `https://github.com/sparkle-project/Sparkle` (from 2.6.0)
   to the APP TARGET ONLY via project.yml (xcodegen packages:) — NOT to
   MeetingKitCore. Wire `SPUStandardUpdaterController` + menu item "Check for Updates…"
   (Settings window footer or menu; your call, note it). Generate
   `App/dsa_pub.pem` placeholder note (ed25519 keygen documented in script README —
   actual keys are a signing-week human step). appcast.xml template under
   `distribution/appcast.xml` with placeholder enclosure + SUEd25519Signature comment.

2. **`scripts/release.sh`** (bash, set -euo pipefail): project → xcodegen, build
   Release (xcodebuild -configuration Release), sign with Developer ID (TEAM_ID +
   SIGNING identity from env vars; documented placeholders), create DMG (hdiutil,
   AJDBZJ-free simple UDZO), notarize (xcrun notarytool submit --wait with keychain
   profile name from env), staple (xcrun stapler). Every secret from env/args; script
   is NO-OP-safe: with missing env it stops early with a clear message listing what's
   needed (dry-run mode prints the plan). Sparkle feed URL from env at build time
   (SUFeedURL in Info.plist via xcodegen variable — document).

3. **`scripts/dev.sh`**: one command for contributors — xcodegen + make build + swift
   test wrapper. Keep trivial.

4. **Root `README.md`** (CREATE — repo has none at root): what Scribe is (one para),
   status (Phase 0 dogfood build), layout map, dev quickstart (scripts/dev.sh,
   requirements: Xcode 26/macOS 14+, xcodegen via brew), test command, signing/
   notarization summary pointing at scripts/release.sh env contract, links: SPEC.md,
   design/, docs/spikes/, TASKS.md. Keep it tight — no marketing.

5. **project.yml edits**: Sparkle package + SUFeedURL placeholder + release settings
   (ASSETCATALOG flags none needed; keep minimal — just what Sparkle/signing need).
   Regenerate project via xcodegen (do NOT commit the xcodeproj — it's gitignored).

6. **`.github/workflows/ci.yml`** (light): on push/PR — brew install xcodegen (cache),
   make project, swift test (package), xcodebuild build CODE_SIGNING_ALLOWED=NO.
   macOS 15 runner (arm64). Keep minimal.

## Constraints
- Do NOT run notarization (no creds); do not modify MeetingKitCore or App Swift logic
  beyond Sparkle menu wiring. `make build` green once at end (with your project.yml).
- Do not commit. Short report.
